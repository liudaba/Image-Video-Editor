server {
    listen 80;
    server_name api.videogen.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.videogen.com;

    ssl_certificate /etc/letsencrypt/live/api.videogen.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.videogen.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        access_log off;
    }

    access_log /var/log/nginx/videogen_access.log;
    error_log /var/log/nginx/videogen_error.log;
}
